# Backend Environment Variables for AWS Lambda
FIREBASE_PROJECT_ID=exchangeratetracker
ENVIRONMENT=production

# Note: Firebase service account JSON should be uploaded as a Lambda layer or stored in AWS Secrets Manager
# The lambda_function.py will look for the service account key at:
# - /opt/firebase-service-account.json (Lambda layer)
# - or via AWS Secrets Manager